base_color = image_blend;  // Store the base color so tinting can be removed
selected = false;  // Determines if the piece has been selected
spaces = [];  // Stores the spaces a selected piece can move to